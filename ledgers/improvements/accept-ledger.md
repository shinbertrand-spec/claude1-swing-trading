# Accept-ledger — Claude1 paper-research self-improvement pilot

Suggest-only. One row per proposal. Disposition is the operator's to set on manual merge
(APPLIED / EDITED / REJECTED); until then it reads PENDING-OPERATOR. This table is the
transferable signal for the self-improvement wait-condition (target: ≥5 proposals, accept-rate
≥~70%, zero wall breaches, zero judge+critic-passed-but-bad-edge).

| # | date | proposal | backtest score | judge | critic (re-ran? verdict) | disposition |
|---|---|---|---|---|---|---|
| 1 | 2026-07-01 | [xs_short_term_reversal re-validation](2026-07-01-xs_short_term_reversal-revalidation.md) — **no change; confirm RETIRE / keep `hold: true`** | RETIRE — net OOS Sharpe **0.50** vs 1.0 gate (gross zero-cost OOS 1.60); full grid {5,10,15} all RETIRE, best OOS 0.67; adverse selection filled 0.19% vs missed 3.99% | **PASS** (4/4 critical) | **re-ran: YES → CLEAN** (reproduced FULL 0.82 / OOS 0.50 within drift; 21-cell split×bottom_n sweep → OOS max 0.83 anywhere) | PENDING-OPERATOR |
| 2 | 2026-07-02 | [xs_short_term_reversal re-validation](2026-07-02-xs_short_term_reversal-revalidation.md) — **no change; confirm RETIRE / keep `hold: true`** _(independent re-execution of the #1 target; see de-dup note below)_ | RETIRE — net OOS Sharpe **0.50** vs 1.0 gate (gross zero-cost OOS 1.60); grid {5,10,15} all RETIRE, best OOS 0.67; adverse selection filled 0.19% vs missed 3.99% | **PASS** (4/4 critical) | **re-ran: YES → CLEAN** (reproduced gross 1.60 / net FULL 0.82 / OOS 0.50 to the decimal; extra split-boundary sweep OOS∈{0.33–0.50}, zero-cost-with-fills OOS 0.55 — RETIRE survived every stress) | PENDING-OPERATOR |
| 3 | 2026-07-02 | [ts_momentum_liquid_us re-validation](2026-07-02-ts_momentum_liquid_us-revalidation.md) — **no change; keep deployed `lookback_days=252`, retain DEPLOY-MARGINAL/FRAGILE + do-not-upsize discipline** _(first LIVE / capital-eligible target — the only live generic deployable)_ | KEEP / DEPLOY-MARGINAL — realistic full-spread FULL Sharpe **1.35** / OOS-agg **1.23** / per-window **3/6 exactly at the 0.5 floor** (weakest pass 0.54); net-gate OOS 1.04 KEEP; in-grid `lookback=126` strictly worse (0.69 / 1-6 → RETIRE) | **PASS** (4/4 critical) | **re-ran: YES → CLEAN** (reproduced all 3 harnesses to the decimal; extra `top_k∈{5,6,8,10,12}` + `atr∈{2,3}` + window-boundary sweeps → deployed 252/top_k=8 provably the best neighbor; no leakage) | PENDING-OPERATOR |
| 4 | 2026-07-26 | [paper-auto live-trade post-mortem](2026-07-26-paper-auto-postmortem.md) — **operational/forensic; 2 mechanics fixes** (floor composer exits at the protective stop + fix P&L bookkeeping) _(NOT a strategy re-validation)_ | Forensic: 9 filled trades net **−$11,176**, 9/9 on now-held setups; ts_momentum filled 0; COIN ratcheted stop gave no protection (gap-aware swing ~$3,321); 3 disagreeing P&L views (−11,176 / 1 / −26,750) | **SUGGEST** (4/4 critical; 2 minor gaps, both since fixed) | **re-ran: YES → DEFECT-FOUND → re-checked CLEAN.** Critic #1 reproduced core numbers + caught Finding-B overstated ~16% (gap-through-stop) → drafter corrected to ~$3,321 + fixed §0 SO / connors date. Fresh blind Critic #2 re-derived every figure to the cent → **CLEAN** (confirmed the gap counterfactual is conservative; surfaced MXL exit-price ambiguity, now folded into Findings B/C) | PENDING-OPERATOR — **eligible: Judge nits fixed + fresh Critic CLEAN** |

> **De-dup note (counting toward the ≥5-proposal wait-condition):** rows #1 and #2 are the **same re-validation target** (`xs_short_term_reversal`) run on consecutive days — #2 independently re-executed all three backtests and reproduced #1 to the decimal. They should count as **ONE** distinct proposal, not two. Row #3 (`ts_momentum_liquid_us`) is a **distinct** target. Row #4 (paper-auto post-mortem) is a **distinct**, operational (non-strategy) proposal. Operator decides whether to collapse rows #1/#2. **Net distinct-proposal count so far: 3** (xs_short_term_reversal · ts_momentum_liquid_us · paper-auto post-mortem). Scorecard: #1–#3 judge-PASS + critic-CLEAN; #4 judge-SUGGEST + **critic-DEFECT-FOUND → drafter corrected → fresh critic CLEAN** (the process worked end-to-end: the blind critic caught a real ~16% overstatement, the drafter fixed it, and a fresh blind critic confirmed the fix). **Zero wall breaches; zero judge+critic-passed-but-bad-edge** (the one defect was CAUGHT and corrected, never passed).
>
> **External-review outcome on #4 (2026-07-27):** an operator-commissioned independent review found **3 substantive defects that both blind critics missed** — SO's +$918 was a phantom (broker-confirmed; true net **−$12,094 / 8 trades**), Finding C's stated cause was wrong (positions.json enumeration, not missing exit_price; Rec C-i was already-done work), and Rec C-iii was already recorded (NFLX +$129,393 bug-artifact, excluded by standing ruling). Review also found a live regression the proposal missed entirely (STP counted as open SELL → composer exits suppressed track-wide). Fixes implemented on operator "go"; post-mortem §8 is the correction record. **Scoring implication: #4's critic-CLEAN was unearned on provenance/prior-art — the critic prompt now carries both passes.** Disposition remains operator's.
>
| 5 | 2026-08-04 | New-strategy research batch (operator asked for 5+ new strategies) — 3 daily candidates tested through the full net-of-cost + walk-forward + per-window + Deflated-Sharpe gate. Artifacts: `2026-08-04-overnight-drift-backtest.(py/md)`, `...-52wk-high-breakout-backtest.(py/md)`, `...-nr7-contraction-breakout-backtest.(py/md)` | **① Overnight drift: FAIL** (breaches DD cap even gross — SPY |MDD| 29%; net@3bps Sharpe 0.77→0.13). **② 52wk-high breakout: FAIL** (net Sharpe 0.41, per-window 1/6, DSR 0.17; confirmed with correct chase-up fill). **③ NR7 contraction breakout: MARGINAL FAIL** — Sharpe **1.00** (needs >1.0), \|MDD\| 11%, per-window **4/6 PASS**, DSR 0.79 (needs >0.95); n=746. The one with a pulse. | self-verified (drafter) | N/A — none cleared, so nothing surfaced to judge/critic | REJECTED (①②); **NR7 = broaden to liquid_us before deciding** |

> **New-strategy note — BATCH COMPLETE (5 ideas + intraday probe, all fail):**
> 1. **Overnight drift** — FAIL (breaches DD cap even gross; net@3bps Sharpe 0.13).
> 2. **52wk-high breakout** — FAIL (net Sharpe 0.41, 1/6 windows).
> 3. **NR7 contraction** — 88-name MARGINAL (Sharpe 1.00 / MDD 11% / 4-of-6); broadened to liquid_us WITH a top-8 trend rank (the ts_momentum-style cap fix) → **FAIL** (agg 0.95 but per-window collapses to 1/6 — years cluster just below the 0.5 floor; DSR 0.69). Breadth did not rescue it.
> 4. **Turn-of-month** — FAIL (best gross Sharpe 0.53 QQQ; the classic calendar effect has decayed 2017-2026; cost not even the issue).
> 5. **PEAD** — NOT BUILT: fundamental PEAD needs historical analyst-estimate data the project lacks; the price-based version ≈ the EP setup, already built + parked. Re-tread/blocked, not a new edge.
> + **ORB intraday probe** (research-only, coarse 1h, ~2yr, NOT gate-able) — negative expectancy net of cost (Sharpe −2.93, hit 47%); does not justify buying an intraday data source.
>
> **Conclusion: none clear the net-of-cost + walk-forward + per-window + DSR gate.** This is the FOURTH independent strategy sweep to reconfirm the project's 2026-06-17 finding — concentrated trend (ts_momentum) is the only durable edge here; momentum / breakout / reversion / seasonality / overnight / intraday all fail. A well-evidenced negative result. Artifacts: `2026-08-04-*.py/.md`. Suggest-only; nothing applied.

> **Critic-surfaced residual risk on #3 (operator eyeball):** the ts_momentum per-window pass is contingent on IS-window length — `is_years=4/5` FAIL on *today's* data, not just a hypothetical future regime. The edge leans on 2020–2021 sitting in the window pool; if the next data refresh rolls 2020 off the front, the gate will very likely stop clearing. Confirm the live-validation kill-criterion is armed to that specific event, not just to a generic drawdown.

| 6 | 2026-08-04 | Intraday/hourly + futures + options exploration (operator asked "hourly strategies in this volatile market" → "futures and options with delta/gamma hedging"). Artifacts: `2026-08-04-hourly-strategies-vol-regime-probe.(py/md)`, `...-gap-fade-daily-proxy-backtest.(py/md)`, `...-gap-fade-paid-feed-scope.md`, `...-variance-risk-premium-probe.(py/md)`, `...-futures-tsmom-longshort-backtest.(py/md)`, `...-options-delta-hedge-backtest.(py/md)`, `...-futures-options-feasibility-scope.md` | **Hourly:** vol DOES help directionally (high-vol gap-fade +0.17%/trade t=1.30) but nothing reaches significance; un-gateable on 2yr coarse data. **Gap-fade daily proxy:** clears the gate in exactly ONE cell (gap≥3% @5bps, |MDD| 24.5% on the cliff); all realistic-cost cells FAIL on drawdown — negative-skew trap. **Futures L/S TSMOM (proxy ETFs, 2010-26):** FAIL — L/S Sharpe 0.15 WORSE than long-only 0.40 (shorts HURT), corr to SPY +0.25 (not the neg diversifier hoped); lone bright spot 2022 crisis-alpha (+0.64 vs SPY −0.78). **Options delta-hedged straddle:** brackets between Sharpe 1.95/MDD 8% (fixed-implied gamma harvest, optimistic — ignores vega spike) and Sharpe 0.31/MDD 72% (variance-swap, pessimistic); truth between — decent carry IF you survive the vega/margin tail that destroys short-vol books. | self-verified (drafter) | N/A — none gate-clears/deployable; options+hourly not gate-able by construction | REJECTED — nothing deployable; scopes filed for operator |

> **Futures/options note (batch 6):** neither is deployable in the current equity-only, long-only stack. **Futures = the edge we already have** (`ts_momentum`-shape trend) on a better instrument — weak on free ETF proxies, adding shorts detracted, real roll-adjusted futures data (~$50-300) needed to confirm the 2022-style crisis-alpha diversification. **Options delta/gamma = a different, negative-skew business** — the variance risk premium is REAL (+3.7 vol pts, 86% monthly hit) but harvesting it is short-gamma insurance-selling; the market already prices the 'volatile market' insight (implied > realized by design), so long-gamma pays carry every calm month. **FIFTH independent sweep reconfirming concentrated trend (ts_momentum) is the only durable edge in this data.** Suggest-only; nothing applied; wall intact.
>
> **Batch 6 follow-on — futures test made turnkey (2026-08-04):** exact 28-contract universe + Databento pull (operator runs; ratio back-adjustment included) + ready-to-run gate backtest that degrades gracefully with no data. Artifacts `2026-08-04-futures-real-data-spec.md` + `...-futures-real-data-backtest.py`. Recommendation: run v1 (skip ICE-softs breadth until v1 warrants); it's the ~$0-30 low-cost tiebreaker for "is there a second edge," expected to fail but worth a clean kill/promote.
>
> **Batch 6 CLOSE — free real-futures pass RAN (2026-08-04), Databento no longer warranted:** ran the futures L/S TSMOM on **Yahoo continuous futures (=F), 34 contracts, 2007-2026 incl. 2008 GFC** (`2026-08-04-futures-yahoo-continuous-backtest.py`) — real futures, not ETF proxies, $0, no key. Roll contamination negligible (0.05% of returns clipped at ±15%). Result: **L/S 12-1 Sharpe 0.30, long-only 0.43, blend −0.03 — ALL FAIL**; adding shorts hurt AGAIN (3rd dataset to agree); corr to S&P **+0.06** (genuine near-zero-corr diversifier). These numbers **match the real managed-futures industry's ~0.3-0.5 Sharpe over this weak-for-trend period** — not a broken test, an honest reproduction. Two real-ish datasets (ETF proxy + Yahoo continuous) now agree, so the clean Databento pull would refine not flip → **do NOT spend the $30.** Futures trend's value is defensive (diversify ts_momentum drawdowns at flat return), a portfolio-construction choice, not a new edge. **SIXTH independent confirmation: concentrated trend (ts_momentum) is the only gate-clearing edge** — and ts_momentum is itself trend, just a better expression (curated equity + top-K rank) than a broad futures book.

| 7 | 2026-08-04 | [ts_momentum fill-path diagnostic](2026-08-04-ts_momentum-fill-path-diagnostic.md) — **operational; the live-validation scope's pre-condition #0** (why ts_momentum has never filled) | Forensic: fill fix (`entry_pricing.py` pivot×1.03 marketable, landed 06-16) is **untested, not broken** — the only post-fix monthly rebalance (~07-15) fell inside the 07-01→07-24 scan outage; AMD/CAT/GOOGL are pre-fix casualties (placed at old pivot×1.001). First real at-bat ~08-13. Residual risks flagged: DAY-TIF+9:35-vs-open (R1), 3%-chase-cap skips best names (R2), monthly clock vs 30-trade bar (R3). | self-verified (drafter) | N/A — operational diagnostic, not a strategy proposal | PENDING-OPERATOR — watch the ~08-13 rebalance end-to-end before any live step |

| 8 | 2026-08-04 | [Insider x momentum experiment](2026-08-04-insider-momentum-experiment.md) — momentum-rank selection on the retired insider KIND (the diagnosed untested flank: selection/capacity, not signal death) | 6 arms, identical accounting loop as the committed best-first experiment; baseline reproduced (0.20 ≈ prior 0.14-0.23 RETIRE). Best arm MOM-GATE Sharpe **0.52** / |MDD| 16.6% / DSR **0.19** — doubles baseline, still half the gate. Momentum-best-first 0.35-0.38 **< conviction-best-first 0.67** (even the degraded conviction layer out-ranks trailing momentum). Gate+best-first: +96% ret but |MDD| 41% (correlated-crash tail). **ALL 6 FAIL.** | self-verified (drafter) | N/A — nothing cleared; pre-registered arms only, no tuning past them | REJECTED — insider KIND stays retired; residual path = rebuild conviction layer (PIT shares + rated insiders), a data-quality project not a selection tweak |

| 9 | 2026-08-06 | [event_earnings_drift Stage A](2026-08-06-earnings-drift-stageA-verdict.md) — full build + trial per the vault handoff (preconditions: filter FROZEN+committed `fffe05f` pre-run; PIT-CIK sidecar 1177/1178, acceptance 96.5%→97.7%; 8 trials registered 83→91 pre-run) | Step 1: **2/8 passed** incl. enforced DSR 0.997 (rank1 OOS Sharpe 1.21). Steps 2–6 (net-of-cost + LIVE fills): **both die** — net wf 0.60/0.49, per-window **0/5**, DSR 0.03/0.01, fill **11%/41%**, missed fwd **+10.0%** vs filled **+1.6%** → **EXECUTION-MODEL MISMATCH** (the pre-registered §2 guard). §2's "inversion" thesis falsified: EAR drift lives in the overnight gap too. | build handoff (operator GO) | chain steps = re-eval of registered combos, no new trials | **RETIRED** — kill rule, no tuning; spec `status: retired`; Stage B not triggered; infra (events pipeline + PIT-CIK resolver) retained |

| 10 | 2026-08-06 | [Same-session entry doctrine review](2026-08-06-same-session-entry-doctrine-review.md) — Task 0 gap-adjusted missed-cohort measurement + §3 analysis (vault handoff; stopping rule pre-registered before computing) | Missed-cohort drift measured from the ACHIEVABLE open (not the pre-gap pivot): earnings-drift +10.0%→**+4.9%**, insider +40.6%→**+32.3%** (net of gap + 20bps) — both **≥2× filled** (2.79×/2.73×) → rule says PROCEED. **Reframe:** price-missed = only 3%/15% of signals; the "11% fill" was CAPACITY-dominated → entry change could NOT have rescued either KIND (correction-of-emphasis to row 9). SDK exposes `auction_limit_order` (LOO); paper capability test: **type AL recognized, session-window gated** (definitive re-run at US RTH pending). | vault handoff | measurement only — no trials, no re-test, no order code, nothing un-retired | **GO — NARROW, narrowed further by the challenge work-through (see addendum)**: LOO = capability SPEC with currently NO demonstrated beneficiary — the ts_momentum-switch recommendation was **WITHDRAWN** after measuring its own cohort (cap-skipped names did WORSE from the open: 0.47× mean / −2.11% median → the +3% cap is adverse-selection PROTECTION for momentum rebalances); earnings-drift excess is tail-driven (median ratio 1.71× < 2×, insider robust 3.08×; Welch t 2.65/1.97); resurrection-guard leak named + norm recorded (perf stat on a retired KIND under a new fill model = trial → manual trials.yml component). Both KINDs stay retired. |

> **Diagnostic note (row 7):** this is the concrete continuation of the operator's "go with your recommendation." Recommendation #1 (execution bugs) was **already fixed** (commit d1955d2, 2080 tests) and #2 (live-validation scope) **already written** (`2026-07-25-...-SCOPE.md`), so the true open item is the scope's own pre-condition #0 — the fill mechanism. The new finding vs the 07-25 scope: the fix has had **zero live at-bats** (its window collided with the scan outage), so it is unproven rather than proven-broken. Do not advance toward live capital until a ts_momentum rebalance FILLS cleanly on the paper path (~08-13 earliest). Suggest-only; wall intact.
