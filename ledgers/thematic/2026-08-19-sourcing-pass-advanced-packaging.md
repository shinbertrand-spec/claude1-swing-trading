# Sourcing pass RETARGET → advanced packaging (2026-08-19)

**Operator direction:** retarget the next sourcing pass to advanced
packaging — the audit's only bottleneck carrying **direct CEO attestation**,
never pursued solely because its expression is not US-listed. Broker reach
is now IBKR (~150 markets), so that constraint is gone.

**Standing scope:** research only; no contact with the execution path or the
09-15 fidelity cycle (which outranks this). Not a trial — `trials.yml` stays
at **103**. Water stays **closed** (2026-08-18, "real mechanism, no
neglected listed expression", spot-check scope). The ownership layer stays
**a question**, unpursued this pass.

---

## THE MECHANISM — stated FIRST, before any screen runs (rules.yml sourcing.screens_do_not_nominate)

> **Each AI accelerator package consumes 5–10× the ABF build-up film of a
> conventional PC chip, and package layer counts are still climbing —
> ~12 layers (H100) → 14 (B200/GB300) → toward 18 (Rubin) — while ~95% of
> that film comes from a single supplier whose announced expansion (~¥50bn
> through 2030) adds only ~50% output, and the largest AI-server substrate
> maker states it has secured film for current guidance but NOT for upside
> while committing ¥500bn over three years to substrate capacity.**

**Quantities, sourced** (search-summary tier this session; every figure
must be re-pulled through its statutory door before any thesis is written —
see rules.yml `verification_doors`):

| Quantity | Value | Note |
|---|---|---|
| ABF film per AI package | **5–10×** a conventional PC chip | the demand multiplier |
| Package layer count | 12 (H100) → 14 (B200/GB300) → ~18 (Rubin) | still rising |
| ABF film supplier share | **~95%, single supplier** (Ajinomoto Fine-Techno) | the choke point |
| That supplier's expansion | **~¥50bn through 2030, ~+50% output** | slower than the multiplier |
| Ibiden substrate capex | **¥500bn (~$3.3bn) over 3 years from FY2026** | scale of the response |
| Ibiden's own constraint | film secured for guidance, **NOT for upside** | the constraint stated by the constrained party |
| CoWoS capacity | ~35k wpm (late-2024) → **120–140k wpm** (end-2026) | the ramp the substrates must feed |
| CoWoS supply-demand gap | ~20% → ~10% by end-2026 | packaging capacity itself is EASING |
| Channel inventory | none — consumed in production | no buffer to draw down |

**What the mechanism claims, precisely:** the substrate/film chain is
capacity-constrained **today**, at the quantities the 2026 CoWoS ramp
already requires. It is a present-state claim with attestation from the
constrained parties, not a forecast.

### 🔴 BLOCKING — the film budget must be reconciled BEFORE any thesis is drafted

Film consumption ≈ **units × package area × layer count**. All three
multiplicands are growing, and they draw on **one constrained input**
against **~+50% announced supply growth to 2030**.

The arithmetic that makes this blocking: **12 → 18 layers is itself ~+50%.**
Content growth ALONE consumes approximately the entire announced expansion —
before a single incremental unit, and before any package-area growth (bigger
interposers, more HBM stacks per package, which pushes the same direction).

Therefore:

- **A thesis claiming BOTH content growth AND unit growth DOUBLE-COUNTS.**
  Both draw on the same film. The pre-registration bars that thesis.
- **The draft must state WHICH the expansion funds** — content (layer/area
  escalation on roughly flat units), or units (volume growth at roughly flat
  content) — and show the arithmetic. It cannot fund both at the announced
  rate.
- If the reconciliation shows the expansion funds **neither** fully (the
  likely case if units grow at all), say so plainly: that is the strongest
  version of the constraint AND simultaneously the tightest ceiling on any
  constraint-holder's volume (see the ceiling clause below).
- **Run it on the RE-PULLED STATUTORY FIGURES**, not the search-summary
  table above: layer counts and film-per-package from the substrate makers'
  own EDINET/TDnet disclosures and the film supplier's own capacity
  statements; units from the accelerator makers' disclosures. A budget
  built on secondary figures is not a budget.
- Record the reconciliation as a table in the thesis file — inputs, source
  door, date — so a future session can re-run it when the capex schedule
  changes.

🔴 **What it must NOT become:** "the bottleneck will migrate from CoWoS to
substrates" is a MIGRATION FORECAST and is banned (handoff §6; `stack-map`
`migration_forecast` was deleted for exactly this reason). Note the
tension honestly — CoWoS's own gap is *narrowing* 20%→10%, which is the
shape of a constraint easing, and a thesis that needs the constraint to
*move* is forecasting. Any thesis from this pass must stand on the
substrate chain being tight **now**, measured, or it does not get written.

### 🔴 THE CEILING — written into the mechanism, not appended as a risk

"Film secured for current guidance but **NOT for upside**" is not colour;
it is **the shape of the trade**. A substrate maker that cannot obtain
incremental film cannot beat its own volume guidance no matter what
accelerator demand does — **its upside is bounded by a third party's capex
schedule** (the film supplier's ~¥50bn/~+50% programme running to 2030).

Consequences, binding on any draft from this pass:

- **This is a MOAT-MISPRICING thesis:** the claim can only be that the
  market underprices the DURABILITY and MARGIN of a position inside a
  choke point whose supply response is externally scheduled and slow —
  i.e. pricing power and mix, on capped volume.
- **THE DEMAND-STORY VERSION IS BARRED, EXPLICITLY.** Any thesis of the
  form "AI accelerator demand grows X%, therefore this name's volumes /
  revenue grow with it" is rejected at draft. The film ceiling caps volume
  *regardless of demand*, and a demand-driven version of this thesis is
  refuted by the very quote the mechanism rests on. Writing both — "the
  constraint is severe" AND "our name's volumes surprise upward" — is the
  same double-count the film budget bars, in narrative form.
- **The upside is therefore someone else's decision.** State, in the
  thesis, what the film supplier's announced capex schedule implies for
  the candidate's volume ceiling by year, and treat any acceleration of
  that schedule as an INVALIDATION trigger (the constraint loosening is
  the thesis dying, on schedule, exactly like FIX's licensure clock).
- Corollary for sizing inputs: a capped-volume, margin-driven thesis has a
  **narrower** upside distribution than a demand-driven one. Do not
  present it with demand-story upside arithmetic.

---

## Where this expression lists (why it was unreachable before)

Substrate and packaging-equipment names are largely **Japanese and
Taiwanese mid-caps** — EDINET/TDnet and TWSE MOPS doors, partial or lagged
English disclosure. Per `rules.yml language_barrier_is_the_edge`, that
lag is the origin of the coverage gap, not an obstacle to route around.

**Tier claim available here: the non-S&P-500-equivalent (neglected)
component ONLY.** Japan and Taiwan are REGIONAL, not local, for a
Singapore-based operator — the +6.9pp local-and-neglected cell is not
claimable on these venues (rules.yml `geography_correction` (a), second
correction).

---

## Pass protocol (order is binding)

1. **Mechanism** — done, above, with quantities.
2. **Re-pull every load-bearing quantity through its statutory door**
   (EDINET/TDnet for Japanese names, TWSE MOPS for Taiwanese) — the table
   above is search-summary tier and is NOT sufficient for a thesis.
3. **Only then screen** for tier (index membership + analyst count,
   measured per name, never inferred from venue) and R1 coldness
   (theme-attention, with a stated proxy — not single-stock drawdown,
   per the withdrawn ERII justification).
4. **Value capture per candidate** — who pays, why this name, why not
   competed away. A choke-point that cannot be held via a listed
   constraint-holder is a fact, not a candidate (the rare-earths lesson).
5. **Adversarial pass** — search the refutation first. Pre-registered
   refutation requirements for any candidate from this pass:
   - **(a) The easing counter:** CoWoS's gap is narrowing 20%→10% and
     Ibiden is spending ¥500bn — capacity IS responding. Why does the
     constraint bind through the response?
   - **(b) The single-supplier trap:** the ~95% film share sits with a
     unit of a large diversified Japanese food-and-chemicals group; the
     choke point may be unbuyable as a pure expression (the
     rare-earths/gallium failure mode — real constraint, wrong
     security).
   - **(c) Already-paid:** substrate and OSAT names have moved with the
     AI complex; `re_rating_status` vs the name's OWN history decides
     whether the constraint is already in the price.
   - **(d) Keystone inheritance:** any thesis here inherits the
     unresolved keystone channel-C firing from birth.
   - **(e) Cyclicality:** substrates are a historically brutal cycle
     (the Patel/Rasgon persona lens — boom-bust with capex-driven
     oversupply). A duration claim must say why this cycle differs, in
     quantities.

**"No qualified candidate" remains a valid outcome of this pass**
(rules.yml `sourcing.no_candidate_is_valid`) — as it was for water.
